pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
                sh 'ls -la'
                sh 'whoami'
            }
        }
    }
}